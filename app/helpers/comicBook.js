class ComicBook {
  constructor(title, cover, description, keywords) {
    this.title = title,
    this.cover = cover,
    this.description = description,
    this.keywords = []
  }
}

export default ComicBook;
