class ComicBook {
  constructor(title, description, cover, id) {
    this.title = title;
    this.description = description;
    this.cover = cover;
    this.id = id;
  }
}

export default ComicBook;
